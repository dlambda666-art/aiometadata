# Custom manifest refresh fix

Existing imported custom catalogs are now refreshed when the same catalog is imported again. The catalog source URL and manifest-derived settings are rebuilt while preserving the user's enabled and Home preferences.
